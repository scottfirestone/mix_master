<h1>All Artists</h1>
<% @artists.each do |artist| %>
  <div>
    <%= link_to artist.name, artist_path(artist.id) %><%= link_to image_tag(artist.image_path), artist_path(artist.id) %>
    <% end %>
  </div>
<%= link_to "New artists", new_artist_path %>
